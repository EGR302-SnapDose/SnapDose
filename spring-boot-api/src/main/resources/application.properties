server.port=${PORT:8080}
spring.application.name=snapdose-api

# Device tokens are read from env vars SNAPDOSE_DEVICES_TAB5_001 and SNAPDOSE_DEVICES_TAB5_002.
# Do NOT set default values here — missing tokens log a warning at startup.
